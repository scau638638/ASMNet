## 文件结构
这里显示的是通道数为16-256的代码，其余通道数原理一样，修改模型代码即可。

model_generator：  模型生成器

ASMNet：编码成模型并训练

cbam：注意力机制cbam

temp_num.txt   &   train_model_num.txt：这里我使用了一种比较蠢的方法，但是也懒得改了，使用temp_num记录第几组模型，train_model_num表示temp_num组模型的第几个模型

util：模型生成器生成的模型编码是存放在.txt文件里，所以写了一个util来读取模型。


